// 我的账户余额
var DM_MyEcoupon = function(){
	MessageMachine.call(this);

	// url
	this.url = url_host+"/my/ecoupon.json";

	// template
    this._template= 
        '{{ecoupon}}';

	// set Render
	this.setRender(this._template);

	
	// 数据格式转换
	this.transfer = function(data){
		if (data) {
		}
		
		return data;
	}
	
	
	// 测试数据
	this.getTestData = function(){
		var response = '';
		return JSON.parse(response);
	}
}
